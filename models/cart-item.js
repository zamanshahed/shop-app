class CartItem {
  constructor(quantity, productPrice, productTitle, totalPrice) {
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.totalPrice = totalPrice;
  }
}

export default CartItem;
